{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Shared, checked-in settings for the whole team. Personal overrides go in settings.local.json (git-ignored). Tune the permission lists to match the commands your project actually runs.",

  "permissions": {
    "allow": [
      "Read(./**)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git branch:*)",
      "Bash(ls:*)",
      "Bash(cat:*)",
      "Bash(grep:*)",
      "Bash(rg:*)",
      "Bash(find:*)"
    ],
    "ask": [
      "Bash(git commit:*)",
      "Bash(git push:*)",
      "Bash(npm install:*)",
      "Bash(pnpm add:*)",
      "Bash(pip install:*)"
    ],
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Read(./**/*.pem)",
      "Read(./**/secrets/**)",
      "Bash(rm -rf:*)",
      "Bash(git push --force:*)",
      "Bash(git reset --hard:*)"
    ]
  },

  "env": {
    "_comment": "Project-wide environment defaults. Do NOT put secrets here.",
    "CLAUDE_PROJECT_KIND": "starter"
  },

  "hooks": {
    "_comment": "Example hooks. Uncomment/adapt to enforce automated behavior. The harness runs these, not Claude.",
    "PostToolUse": [
      {
        "_comment": "Auto-format files after Claude edits them. Replace with your formatter.",
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Tip: wire your formatter here, e.g. prettier --write or ruff format'"
          }
        ]
      }
    ]
  }
}
