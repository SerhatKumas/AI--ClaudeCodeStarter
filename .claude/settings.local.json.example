{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Copy this to settings.local.json for PERSONAL overrides. This file is git-ignored. Use it for machine-specific permissions or your preferred model — never commit secrets.",
  "permissions": {
    "allow": [
      "Bash(pnpm test:*)",
      "Bash(pnpm dev:*)"
    ]
  }
}
